import { BondDirective } from './bond.directive';

describe('BondDirective', () => {
  it('should create an instance', () => {
    const directive = new BondDirective();
    expect(directive).toBeTruthy();
  });
});
